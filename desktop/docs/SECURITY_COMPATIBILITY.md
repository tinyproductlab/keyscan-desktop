# 跨平台安全兼容规则

Windows/macOS 开发前必须先与 Android/iOS 固定以下协议：

- PIN 的 UTF-8 编码和规范化规则
- 数据保护密钥的大小写、空格和长度规则
- 密钥派生算法、盐值、迭代次数及内存参数
- 加密算法、密钥长度、Nonce/IV 和认证标签长度
- 数据库与安全备份的版本字段
- TOTP、密码账本、安全保险箱和附件的数据结构
- WebDAV 目录和文件命名规则
- 备份升级与旧版本兼容策略

禁止直接复制 Android 数据库作为桌面数据格式。四个平台应通过版本化的 KeyScan 安全备份格式迁移和同步。

